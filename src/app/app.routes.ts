import { Routes } from '@angular/router';
import { CaptchaViewComponent } from './captcha-view/captcha-view.component';

export const routes: Routes = [
    { path: '', component: CaptchaViewComponent },
];

